import React from 'react';
import './App.css';

const App: React.FC<{}> = () => {
  // 以下の手順で実装していく
  // 名前と生年月日を入力するフォームと送信ボタンを作成する
  // 名前と生年月日をStateで管理する
  // 送信ボタンを押したら、APIを叩いて、dataを取得する
  // 取得したdataを表示する

  return (
    <div className="App">
      <h1>Hello World</h1>
    </div>
  );
}

export default App;
